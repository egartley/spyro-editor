<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Spyro_Editor.Views.SubfileHexViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Spyro_Editor.Views"
    xmlns:hex="using:BetterHexViewer.WinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <hex:BetterHexViewer x:Name="HexViewer" Margin="0,0,0,12" ColumnGroupSize="Four" FullWidth="True" />
        <Grid Grid.Row="1" Padding="8" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundChromeBlackLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="ExportButton" Content="Export" Click="ExportButton_Click" />
            <TextBlock x:Name="TotalSizeText" Grid.Column="1" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>
