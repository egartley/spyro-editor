<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Spyro_Editor.Views.SubfileOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Spyro_Editor.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="12">
            <TextBlock Text="{x:Bind Model.Title, Mode=OneWay}" Margin="0,0,0,16" Style="{ThemeResource SubheaderTextBlockStyle}" />
            <Grid Margin="0,0,0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Type: " Style="{ThemeResource BodyLargeStrongTextBlockStyle}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Level: " Style="{ThemeResource BodyLargeStrongTextBlockStyle}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Parts: " Style="{ThemeResource BodyLargeStrongTextBlockStyle}" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Textures: " Style="{ThemeResource BodyLargeStrongTextBlockStyle}" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Moby Instances: " Style="{ThemeResource BodyLargeStrongTextBlockStyle}" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind Model.Type, Mode=OneWay}" Margin="12,0,0,4" Style="{ThemeResource BodyLargeTextBlockStyle}" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind Model.DetectedLevel, Mode=OneWay}" Margin="12,0,0,4" Style="{ThemeResource BodyLargeTextBlockStyle}" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind Model.PartCount, Mode=OneWay}" Margin="12,0,0,4" Style="{ThemeResource BodyLargeTextBlockStyle}" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{x:Bind Model.TextureCount, Mode=OneWay}" Margin="12,0,0,4" Style="{ThemeResource BodyLargeTextBlockStyle}" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{x:Bind Model.MobyInstanceCount, Mode=OneWay}" Margin="12,0,0,4" Style="{ThemeResource BodyLargeTextBlockStyle}" />
            </Grid>
            <TextBlock Text="This UI is not finished" Foreground="{ThemeResource TemporaryTextFillColorDisabled}" />
        </StackPanel>
    </Grid>
</Page>
