<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Spyro_Editor.Views.SubfilePane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Spyro_Editor.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="12,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Margin="0,0,0,12" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundChromeBlackLowBrush}">
            <SelectorBar x:Name="SelectorBar" SelectionChanged="SelectorBar_SelectionChanged">
                <SelectorBarItem x:Name="OverviewSelectorItem" Padding="24,8" Text="Overview" IsSelected="True" />
                <SelectorBarItem x:Name="HelixViewSelectorItem" Padding="24,8" Text="3D View" />
                <SelectorBarItem x:Name="TextureGallerySelectorItem" Padding="24,8" Text="Textures" />
                <SelectorBarItem x:Name="HexDataViewSelectorItem" Padding="24,8" Text="Raw Data" />
            </SelectorBar>
        </StackPanel>
        <Frame x:Name="ContentFrame" Grid.Row="1" IsNavigationStackEnabled="False" />
    </Grid>
</Page>
