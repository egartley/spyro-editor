<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Spyro_Editor.Views.MobyInstances"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Spyro_Editor.Views"
    xmlns:model="using:Spyro_Editor.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="MobyInstanceItemTemplate" x:DataType="model:MobyInstanceItem">
            <ItemContainer>
                <StackPanel Padding="12">
                    <TextBlock Text="{x:Bind Title}" Style="{ThemeResource BodyLargeTextBlockStyle}" />
                    <TextBlock Text="{x:Bind Subtitle}" />
                    <TextBlock Text="{x:Bind Subtitle2}" />
                </StackPanel>
            </ItemContainer>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock x:Name="HeaderTextBlock" Margin="0,0,32,0" Style="{ThemeResource SubheaderTextBlockStyle}" />
            <TextBlock Text="Filter controls will go here" VerticalAlignment="Center" Foreground="{ThemeResource TemporaryTextFillColorDisabled}" />
        </StackPanel>
        <ItemsView Grid.Row="1" ItemsSource="{x:Bind Model.Items}" ItemTemplate="{StaticResource MobyInstanceItemTemplate}" IsItemInvokedEnabled="False" SelectionMode="None" />
    </Grid>
</Page>
